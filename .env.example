# Copy this file to .env.local and set your private sign-in credentials.
# Do not prefix any of these values with NEXT_PUBLIC_.
AUTH_EMAIL=
# Use a unique password of at least 12 characters.
AUTH_PASSWORD=
# Generate with: openssl rand -hex 32
AUTH_SECRET=
